import React from 'react';
import { useState } from 'react';

export default function MyComponent() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <h1>Hello World</h1>
      <p>Count: {count}</p>
    </div>
  );
}

# This is a heading

This is a paragraph with **bold** and *italic* text.

Here's a `code span` and a [link](https://example.com).

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}
```

<CustomComponent prop="value">
  ## Nested heading
  
  This is content inside a JSX component.
  
  - List item 1
  - List item 2
  
  {/* This is a comment */}
  
  <NestedComponent>
    The count is: {count}
  </NestedComponent>
</CustomComponent>

> This is a blockquote
> with multiple lines.

| Column 1 | Column 2 |
|----------|----------|
| Cell 1   | Cell 2   |
| Cell 3   | Cell 4   |

---

Final paragraph with {dynamicValue} expressions.